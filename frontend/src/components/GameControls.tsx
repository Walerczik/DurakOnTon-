const GameControls = () => {
  return (
    <div className="controls" style={{ marginTop: '20px' }}>
      <button>Отбой</button>
      <button>Беру</button>
    </div>
  );
};

export default GameControls;
